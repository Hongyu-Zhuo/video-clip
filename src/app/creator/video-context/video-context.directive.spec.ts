import { VideoContextDirective } from './video-context.directive';

describe('VideoContextDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoContextDirective();
    expect(directive).toBeTruthy();
  });

  it('should create a canvas element after initCanvas method executed', () => {
    expect()
  });
});
