import { DragSourceDirective } from './drag-source.directive';

describe('DragSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new DragSourceDirective();
    expect(directive).toBeTruthy();
  });
});
